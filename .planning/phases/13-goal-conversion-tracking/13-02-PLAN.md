---
phase: 13-goal-conversion-tracking
plan: 02
type: tdd
wave: 2
depends_on: [13-01]
files_modified:
  - packages/lumina-core/src/Services/AnalyticsService.php
  - packages/lumina-core/tests/Unit/AnalyticsServiceTest.php
autonomous: true
requirements: [REQ-GOAL-02]
must_haves:
  truths:
    - AnalyticsService accurately computes goal conversion metrics
  artifacts:
    - packages/lumina-core/src/Services/AnalyticsService.php
  key_links:
    - AnalyticsService queries lumina_events matching goal target_values
---

<objective>
Implement goal conversion calculations in AnalyticsService.
Purpose: Provide accurate conversion counts, rates, and trends to power the dashboard.
Output: Working, tested AnalyticsService goal methods.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Goal Conversion Calculations</name>
  <files>packages/lumina-core/src/Services/AnalyticsService.php, packages/lumina-core/tests/Unit/AnalyticsServiceTest.php</files>
  <behavior>
    - getGoals() fetches site's goals and returns array of goal metrics
    - Method computes `completions` from events table (matching target path or custom event name)
    - Method computes `conversion_rate` relative to unique visitors
    - Method computes a `trend` array (daily completions)
    - Overview payload includes goals data
    - clearCache() clears goals cache
  </behavior>
  <action>Update AnalyticsService to support getGoals. Iterate over site goals, query events table, and calculate required metrics. Add to getOverview.</action>
  <verify>
    <automated>php artisan test --filter AnalyticsServiceTest</automated>
  </verify>
  <done>AnalyticsService correctly returns goal metrics.</done>
</task>

</tasks>

<output>
Create `.planning/phases/13-goal-conversion-tracking/13-02-SUMMARY.md` when done
</output>
