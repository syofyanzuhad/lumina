---
phase: 13-goal-conversion-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lumina-core/database/migrations/*_create_goals_table.php
  - packages/lumina-core/src/Models/Goal.php
  - packages/lumina-core/src/Models/Site.php
  - app/Http/Controllers/GoalController.php
  - routes/web.php
  - tests/Feature/GoalTest.php
autonomous: true
requirements: [REQ-GOAL-01]
must_haves:
  truths:
    - Site owners can create, read, update, and delete goals via API
  artifacts:
    - packages/lumina-core/src/Models/Goal.php
    - app/Http/Controllers/GoalController.php
  key_links:
    - Goal is associated with Site through site_id
---

<objective>
Implement Goal schema, Goal model, and resourceful GoalController for managing conversion goals.
Purpose: Create the foundational database and API layers to support goal tracking.
Output: Goal migration, model, CRUD controller and routes.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-goal-conversion-tracking/13-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: End-to-end Goal GET Slice</name>
  <files>packages/lumina-core/database/migrations/*_create_goals_table.php, packages/lumina-core/src/Models/Goal.php, packages/lumina-core/src/Models/Site.php, app/Http/Controllers/GoalController.php, routes/web.php, tests/Feature/GoalTest.php</files>
  <action>Create the goals table migration (site_id, name, target_type, target_value). Create the Goal model and add the hasMany goals relationship to Site. Wire up a standard GET route for /sites/{site}/goals and implement it in GoalController. Write a Feature test in GoalTest to verify it.</action>
  <verify>
    <automated>php artisan test --filter=GoalTest</automated>
  </verify>
  <done>The database schema is migrated and GET requests return the site's goals.</done>
</task>

<task type="auto" tdd="true">
  <name>Goal Management CRUD API</name>
  <files>app/Http/Controllers/GoalController.php, tests/Feature/GoalTest.php</files>
  <behavior>
    - POST /sites/{site}/goals creates a new goal
    - PUT /sites/{site}/goals/{goal} updates an existing goal
    - DELETE /sites/{site}/goals/{goal} removes a goal
    - Ensure site ownership is validated
  </behavior>
  <action>Implement store, update, and destroy methods in GoalController. Use standard Laravel route model binding and validation rules (name: required, target_type: in:path,custom_event, target_value: required).</action>
  <verify>
    <automated>php artisan test --filter=GoalTest</automated>
  </verify>
  <done>Full CRUD operations work securely for goals.</done>
</task>

</tasks>

<verification>
- `goals` table exists
- Model relationships are set up
- Goal API routes are reachable and pass validation tests
</verification>

<success_criteria>
Goals can be fully managed via API endpoints for a given site.
</success_criteria>

<output>
Create `.planning/phases/13-goal-conversion-tracking/13-01-SUMMARY.md` when done
</output>
