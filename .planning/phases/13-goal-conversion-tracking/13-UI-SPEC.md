---
phase: 13
slug: goal-conversion-tracking
status: draft
shadcn_initialized: true
preset: new-york-v4
created: 2026-07-30
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn-vue |
| Preset | new-york-v4 |
| Component library | radix-vue |
| Icon library | lucide |
| Font | Instrument Sans |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 14px | 600 | 1.5 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(0 0% 100%) | Background, surfaces |
| Secondary (30%) | hsl(0 0% 98%) | Cards, sidebar, nav |
| Accent (10%) | hsl(0 0% 96.1%) | Buttons hover, specific interactive elements |
| Destructive | hsl(0 84.2% 60.2%) | Destructive actions only |

Accent reserved for: Primary CTA buttons, active states, hover backgrounds

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Create Goal |
| Empty state heading | No goals created yet |
| Empty state body | Set up goals to track conversions for specific paths or custom events. Create your first goal. |
| Error state | Failed to load goals. Please try again. |
| Destructive confirmation | Delete Goal: Are you sure you want to delete this goal? This action cannot be undone. |

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5) and lifted by plan-phase's
> `## UI Considerations` lift rule via the identical rule as SPEC `## Edge Coverage`. Shape-rooted UI *state*
> coverage (empty / loading / error / populated / partial / overflow / zero-one-many / long-text).
> Empty-state and error-state COPY live in `## Copywriting Contract` above — this section covers
> state coverage and REFERENCES those rows rather than restating the copy (de-dup).

Applicable state considerations resolved: 2 covered, 1 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | goals list | ✅ covered | Empty results render the documented 'No goals created yet' copy |
| long-text | goal name | 🧪 backstop | { statement: "Long goal names truncate gracefully", verification: "backstop" } |
| overflow | goals container | ✅ covered | Container allows vertical scrolling |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Button, Input, Select, Dialog, Form, Badge | not required |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
