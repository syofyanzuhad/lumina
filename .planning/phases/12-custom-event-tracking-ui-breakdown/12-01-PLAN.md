# Phase 12: Custom Event Tracking UI & Breakdown - Plan 01

## Goals
- Extend `AnalyticsService` for custom event tracking UI
- Add UI components for custom event breakdowns and metadata inspection in Inertia and Livewire
- Ensure parity between Inertia and Livewire components

<task>
<name>1. Extend AnalyticsService with Custom Event Breakdown Methods</name>
<files>
- packages/lumina-core/src/Services/AnalyticsService.php
- packages/lumina-core/tests/Feature/AnalyticsServiceTest.php
</files>
<action>
Extend `Lumina\Core\Services\AnalyticsService` with custom event breakdown methods:
- `getCustomEventSummary(Site $site, CarbonInterface $start, CarbonInterface $end, ?string $selectedEvent = null): array`
- `getCustomEventsList(Site $site, CarbonInterface $start, CarbonInterface $end): Collection`
- `getCustomEventTimeline(Site $site, CarbonInterface $start, CarbonInterface $end, ?string $eventName = null): Collection`
- `getCustomEventPropertyKeys(Site $site, string $eventName, CarbonInterface $start, CarbonInterface $end): array`
- `getCustomEventPropertyBreakdown(Site $site, string $eventName, string $propertyKey, CarbonInterface $start, CarbonInterface $end, int $limit = 10): Collection`
- `getCustomEventLogs(Site $site, CarbonInterface $start, CarbonInterface $end, ?string $eventName = null, int $limit = 50): Collection`

Add unit/feature tests for all new methods in `packages/lumina-core/tests/Feature/AnalyticsServiceTest.php` using Pest.
</action>
<verify>
Run Pest tests: `vendor/bin/pest packages/lumina-core/tests/Feature/AnalyticsServiceTest.php`
</verify>
<done>true</done>
</task>

<task>
<name>2. Inertia Custom Events Dashboard UI & Controller Integration</name>
<files>
- app/Http/Controllers/DashboardController.php
- resources/js/pages/Dashboard.vue
- resources/js/components/CustomEventsTab.vue
- tests/Feature/DashboardControllerTest.php
</files>
<action>
Update `DashboardController.php` to fetch custom event data using `AnalyticsService` and pass props to Inertia.
Update `Dashboard.vue` and create `CustomEventsTab.vue` to add the Custom Events Tab with event selector dropdown, KPI summary cards, custom event timeline bar chart, top events breakdown progress bars, metadata property key inspector, and recent custom event logs stream with raw JSON metadata viewer.

Add tests in `tests/Feature/DashboardControllerTest.php` to verify custom event props and tab switching.
</action>
<verify>
Run Pest tests: `vendor/bin/pest tests/Feature/DashboardControllerTest.php`
</verify>
<done>true</done>
</task>

<task>
<name>3. Livewire Custom Events Dashboard UI Component Integration</name>
<files>
- packages/lumina-core/src/Livewire/Dashboard.php
- packages/lumina-core/resources/views/livewire/dashboard.blade.php
- packages/lumina-core/tests/Feature/LivewireDashboardTest.php
</files>
<action>
Update Livewire dashboard component `Dashboard.php` and view `dashboard.blade.php` to add an identical Custom Events Tab, filter state (`$activeTab`, `$selectedEvent`, `$selectedPropertyKey`), timeline bar chart, breakdown grid, metadata inspector, and raw log JSON viewer with 100% visual and functional parity to Inertia.

Add tests in `packages/lumina-core/tests/Feature/LivewireDashboardTest.php` using Pest to verify Livewire tab state, event selection, and property breakdown rendering.
</action>
<verify>
Run Pest tests: `vendor/bin/pest packages/lumina-core/tests/Feature/LivewireDashboardTest.php`
</verify>
<done>false</done>
</task>
