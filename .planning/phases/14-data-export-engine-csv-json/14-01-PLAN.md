---
phase: 14-data-export-engine-csv-json
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - app/Http/Controllers/ExportController.php
  - tests/Feature/ExportControllerTest.php
  - resources/js/Pages/Dashboard.vue
autonomous: true
requirements:
  - REQ-EXPORT-01
  - REQ-EXPORT-02
must_haves:
  truths:
    - Users can download streamed CSVs of their pageview events.
    - Users can download streamed JSON of their events.
    - Exports strictly respect the active site and date range filters.
  artifacts:
    - path: app/Http/Controllers/ExportController.php
      provides: Export functionality for CSV and JSON
      min_lines: 50
  key_links:
    - from: ExportController
      to: DashboardController
      via: Uses the same date resolution logic
---

<objective>
Implement the streaming Export Engine for CSV and JSON data.

Purpose: Allow users to extract raw and aggregated analytics safely and efficiently.
Output: ExportController with chunked CSV and JSON streaming support.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-data-export-engine-csv-json/14-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>End-to-end "Pageview CSV Export" — one path only</name>
  <files>routes/web.php, app/Http/Controllers/ExportController.php, resources/js/Pages/Dashboard.vue, tests/Feature/ExportControllerTest.php</files>
  <action>Create `ExportController` with an `export` method. Implement the logic to stream Pageviews (events where metadata is null) as CSV chunking by 1000. Apply date resolution from `request()->get('period')` and start/end dates, identical to DashboardController logic. Register the `sites.export` route to this new controller (replacing the old logic in SiteController), and explicitly add the `auth` and `can:view,site` middleware to enforce authorization. In `ExportControllerTest`, add assertions to check for 403/401 responses when unauthorized. In Dashboard.vue, update the existing export button to target this new endpoint with the current date parameters.</action>
  <verify>
    <automated>php artisan test --filter=ExportControllerTest</automated>
  </verify>
  <done>The single happy path works end-to-end and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Expand format & type support (JSON, Events, Summary)</name>
  <files>app/Http/Controllers/ExportController.php, tests/Feature/ExportControllerTest.php</files>
  <action>Expand `ExportController@export` to support `type` (pageviews, events, summary) and `format` (csv, json). For `type=events`, filter `whereNotNull('metadata')`. For `format=json` (pageviews/events), implement a custom stream response that outputs `[` initially, chunks through records emitting `json_encode($row)`, and closes with `]`. For `type=summary`, return `AnalyticsService::getOverview()` directly as JSON, or explicitly flatten the metrics into CSV headers/rows if CSV is requested.</action>
  <verify>
    <automated>php artisan test --filter=ExportControllerTest</automated>
  </verify>
  <done>Export endpoint can serve JSON streams and handle distinct event types without exhausting memory.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Export endpoints receive potentially unbounded date ranges or unauthorized site IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | ExportController | critical | mitigate | Enforce `auth` and `can:view,site` middleware/gates on export routes |
| T-14-02 | Denial of Service | ExportController | medium | mitigate | Enforce chunking and streamed responses rather than loading all into memory |
</threat_model>

<verification>
Automated tests pass for the controller streaming logic and authorization.
</verification>

<success_criteria>
Export API correctly streams filtered data in CSV/JSON without exhausting PHP memory limits.
</success_criteria>

<output>
Create `.planning/phases/14-data-export-engine-csv-json/14-01-SUMMARY.md` when done
</output>
