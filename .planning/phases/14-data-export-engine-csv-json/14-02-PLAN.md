---
phase: 14-data-export-engine-csv-json
plan: 2
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - resources/js/Pages/Dashboard.vue
  - resources/views/livewire/dashboard.blade.php
autonomous: false
requirements:
  - REQ-EXPORT-03
must_haves:
  truths:
    - Users can select the export type and format from the UI.
    - Both Vue and Livewire dashboards have parity in export functionality.
  artifacts:
    - path: resources/js/Pages/Dashboard.vue
      provides: Vue dashboard UI with export dropdown
      min_lines: 100
  key_links:
    - from: UI dropdown
      to: export route
      via: cleanly appends query parameters
---

<objective>
Integrate the Export API with the Vue and Livewire dashboard UIs.

Purpose: Allow users to easily choose which data and format they want to export.
Output: Export dropdown/modal in both dashboard views.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-data-export-engine-csv-json/14-RESEARCH.md
@.planning/phases/14-data-export-engine-csv-json/14-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Vue UI Export Menu</name>
  <files>resources/js/Pages/Dashboard.vue</files>
  <action>Replace the existing simple Export button with a DropdownMenu (using shadcn-vue). The dropdown should provide options for Type (Pageviews, Custom Events, Overview) and Format (CSV, JSON). Upon selection, enter a loading state with spinner on the button (per UI-SPEC), fetch the stream or trigger a window download using `type`, `format`, `period`, `start_date`, and `end_date` parameters. Show standard error toast if export initiation fails.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Vue dashboard supports full export options with UI loading/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Livewire UI Export Menu</name>
  <files>resources/views/livewire/dashboard.blade.php</files>
  <action>Implement the equivalent export menu in the Livewire embedded dashboard. Use standard Blade/Alpine dropdown components to offer the same Type and Format options, redirecting to the `sites.export` route with the active date range filters. Ensure visual parity where possible.</action>
  <verify>
    <automated>php artisan view:cache</automated>
  </verify>
  <done>Livewire dashboard supports full export options.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Export UI integrated into both Vue and Livewire dashboards.</what-built>
  <how-to-verify>
    1. Visit the Vue dashboard and use the export menu to download Pageviews (CSV).
    2. Try downloading Custom Events (JSON).
    3. Visit the embedded Livewire dashboard and verify the export menu functions identically.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | User initiates export action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Information Disclosure | UI Components | low | accept | UI simply constructs links; actual authorization is enforced on the backend |
</threat_model>

<verification>
Human verification of UI interactions and downloaded files.
</verification>

<success_criteria>
Users can successfully export specific formats and types from the frontend dashboards via a polished UI.
</success_criteria>

<output>
Create `.planning/phases/14-data-export-engine-csv-json/14-02-SUMMARY.md` when done
</output>
