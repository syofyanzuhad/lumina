---
phase: 16
plan: 01
type: executable
wave: 1
depends_on: []
files_modified:
  - tests/Feature/MilestoneV11Test.php
requirements:
  - REQ-VFY-01
---

# Master E2E Feature Integration & Zero Regression Test Suite

<must_haves>
- A new file `tests/Feature/MilestoneV11Test.php` exists and executes successfully.
- The master E2E test runs through all new v1.1 features sequentially.
- Running the regression test suite across the entire project (`php artisan test --compact`) returns 100% green without regressions.
</must_haves>

<threat_model>
- **False Positives**: Ensure the master E2E test verifies actual database state and response content instead of just 200 OK statuses.
- **Flaky Tests**: Use synchronous queue driver (`Queue::fake()`) or explicit processing so async event jobs are deterministic.
</threat_model>

<task>
<action>
Create `tests/Feature/MilestoneV11Test.php` implementing full persona lifecycle.

1. **Ingest -> Enhanced Detection**: Simulate tracking an event with specific User-Agent and GeoIP info.
2. **Queue Async Worker**: Verify the event tracking job is queued and/or processed.
3. **Goal Metrics**: Simulate conversion tracking and ensure metrics update properly.
4. **Streaming Data Export**: Request data export endpoints (CSV/JSON) and verify streaming output structure.
5. **Public Share Dashboard & Password Authentication**: Validate that accessing the dashboard using public share tokens and password logic functions correctly.
</action>
<acceptance_criteria>
- `MilestoneV11Test.php` covers the complete end-to-end integration scenario for Milestone v1.1 capabilities.
</acceptance_criteria>
<verify>
php artisan test --compact --filter=MilestoneV11Test
</verify>
</task>

<task>
<action>
Execute regression suite (`php artisan test --compact`) ensuring all 137+ tests pass.

Ensure all previous unit, feature, and integration tests created in Milestone v1.0 and v1.1 are passing with zero regressions or dropped assertions.
</action>
<acceptance_criteria>
- The full test suite runs without errors.
- 137+ tests pass successfully.
</acceptance_criteria>
<verify>
php artisan test --compact
</verify>
</task>
